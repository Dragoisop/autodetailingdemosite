import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "E and E's Detailing - Premium Auto Detailing Services",
  description:
    "Professional, affordable, and reliable auto detailing services. Make your car shine like new with our premium detailing packages.",
  generator: "v0.app",
  keywords: "auto detailing, car wash, ceramic coating, paint correction, interior detailing, exterior detailing",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
